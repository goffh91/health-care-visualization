import { all } from 'redux-saga/effects';
// import { forwardTo } from '../../utils/history';
// import * as appTypes from './types';
// import { getApp } from './actions';

export default function* inquireSaga() {
    yield all([

    ]);
}